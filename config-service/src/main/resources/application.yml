server:
  port: 9000
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/edwin/Documents/Clase-Jueve/3.Multiproyecto-main/Multiproyecto-config
  security:
    user:
      name: ${CONFIG_NAME}
      password: ${CONFIG_PASSWORD}
